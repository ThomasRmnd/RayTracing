#include "sphere.h"

sphere::sphere(const vector3& _position, float _radius) :
	position(_position),
	radius(_radius)
{}

std::vector<vector3> sphere::intersection()
{
	std::vector<vector3> test;
	return test;
}