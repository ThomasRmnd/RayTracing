#include "../../include/object.h"

std::vector<vector3> object::intersection(const rayon& _rayon)
{
	(void)_rayon;
	std::vector<vector3> result;
	result.push_back(vector3());
	return result;
}

vector3 object::vector_normal(const vector3& v)
{
	(void)v;
	return vector3();
}